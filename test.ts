// import "dotenv/config";
// import { connectWithRetry } from "@/lib/db";

// connectWithRetry()
//   .then(() => console.log("✅ Connection test passed"))
//   .catch((error) => console.error("❌ Connection test failed:", error));